pages:
  stage: deploy
  image:
    name: donaldrich/mkdocs:latest
    entrypoint: [""]
  script:
  - mkdocs build
  - mv site public
  artifacts:
    paths:
    - public
  rules:
    - exists:
      - "mkdocs.yml"
      when: on_success
    - when: never
