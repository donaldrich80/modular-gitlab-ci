ansible:
  extends: .ansible-runner
  stage: lint
  script:
    - ansible-lint --force-color -x 204 -x 702 *.yml
  allow_failure: true
  interruptible: true
  when: on_success
  # rules:
  #   - if: '$DEBUG_PIPELINE'
  #     when: never
  #   - changes:
  #     - "**/playbooks/*.yml"
  #     when: always
  #   - exists:
  #     - "**/playbooks/*.yml"
  #     when: manual
  #   - when: never

# ansible-lint  -p -v

